# -*- mode: python -*-
a = Analysis(['.\\example_client.py'],
             pathex=['C:\\Users\\Sarah\\Documents\\GitHub\\TUFeverywhere\\Project'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='example_client.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='example_client')
